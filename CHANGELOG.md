# 更新日志

本文件记录 SayIt 各版本的功能变更、Bug 修复与优化。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

变更分类：`新增` / `修复` / `优化` / `变更` / `移除` / `安全`。

---

## [0.1.4] - 2026-08-06

> 本版本最大的变化是本地识别整体换代，以及设置与个性化相关页面的一轮重做。

### 新增
- **本地识别换用全新引擎，速度快一个量级，可选模型从 2 个扩到 5 个**（按从快到准排列）：
  - SenseVoice Small —— 极快，日常首选
  - Fun-ASR Nano（新增）—— 又快又准，中文口述首选
  - Qwen3-ASR 0.6B —— 较快，多语种轻量之选
  - Qwen3-ASR 1.7B（新增）—— 准，且比高精度档更小更快
  - Qwen3-ASR 1.7B 高精度 —— 最准，但明显更慢
- **本地识别支持显卡加速**：自动检测可用 GPU，有显卡时优先使用；也可手动切到 CPU。
- **快捷键冲突检测**：按住说话、免提、润色模式切换键不能绑同一个键，冲突时当场提示。
- **「按住说话」支持 Ctrl + Win 这类纯修饰键组合。**
- **配置备份支持按需选择**：可只导出热词组、文本替换或润色模式，导入前预览变更。
- **应用 Prompt 规则支持自己添加**：除内置应用外，可为任意程序新建规则并单独指定预设与附加提示词。点「检测当前应用」后切到目标程序，会自动填入它的进程名，不用手输。
- **新建润色模式更好上手**：表单从列表底部移到上方（点「新建」不再像没反应），可选「以现有模式为模板」把提示词填好再改，保存按钮不可点时会说明缺什么。
- **文本替换、自定义热词组、自定义润色模式支持拖动排序**：拖动左侧手柄即可把常用的挪到前面，不必再翻到列表底部（也可聚焦手柄后用方向键调整）。其中文本替换的顺序就是执行顺序（前一条替换后的结果会参与后一条匹配），热词组顺序会影响超出上限时谁被保留，界面上都已注明。

### 优化
- **「AI 供应商」页重做成「AI 服务」卡片列表**：一张卡就是一份完整配置（供应商 + 地址 + 密钥 + 模型），点一张即整份生效，可存多份、同屏比较。原有配置自动迁移，旧数据不会丢。
- **每份 AI 服务可一键测试可用性**：显示能否连通、以及可用时一次往返多快（按快慢分档着色，太慢会提示不建议用于口述）；结果过期会自动提示重测。
- **服务器模式下「AI 服务」页会明确标注不生效**：这页的配置在服务器模式下不参与（AI 整理由服务器完成），页面顶部会标出来；配置与测试仍可正常进行，切到云 API 或本地模式后即生效。
- **多处「保存 / 测试」按钮合并为「保存并测试」**：此前测试用的是未保存的值，看到成功却其实没保存生效。改动未保存时会有明确标记。
- **报错改成人话并给出下一步**：区分网址错、没联网、密钥被拒、模型未开通、限流欠费等，不再直接甩原始异常。
- **状态显示不再说谎**：语音引擎页、左下角引擎指示会按真实条件显示「就绪 / 待配置」，不再一律显示绿灯。
- **全应用把「AI 校对」「AI 润色」统一叫「AI 整理」。**
- **「格式规范」仅在关闭 AI 整理时生效**：开启 AI 整理后排版交给 AI，避免两边冲突；文本替换不受影响，始终生效。
- **本地模式设置分层**：常用的选模型、识别测试放在前面，识别语言、计算后端等收进「高级设置」。
- **设置页与配色打磨**：麦克风卡片上移、状态色对比度达标、键盘与读屏可用性完善、弹窗支持 Esc 关闭。
- **本地识别的等待时间进一步压缩**：裁掉首尾静音、启动预热模型，第一句口述更快；切换识别语言不再重新加载模型。
- **历史记录鼠标悬停即展开详情**：不用再点箭头，移开自动收起；正在编辑或播放录音时不会收起。展开也不再时上时下地跳动。
- **离线下载指引更好用**：弹窗加宽，每个模型的下载链接完整显示、不再被截断，方便手动下载。
- 本地模型空闲卸载时间从 15 分钟延长到 60 分钟。

### 修复
- 修复新建润色模式后立即选择、实际整理仍可能回退到内置模式的问题。
- 修复增删或启停热词后需重启才生效的问题，现在下一次录音即生效。
- 修复录制快捷键时按到已绑定的键（如右 Alt）会直接触发录音、而非录入设置的问题。
- 修复本地模式下模型未下载时仍能开始录音的问题：设置页写着「按下快捷键不会有反应」，实际却照常录音、录完才在识别阶段报错。现在按快捷键会直接提示「模型未下载，请先在设置里下载」，不再白录一遍；下载或删除模型后就绪状态也会立即更新。
- 修复设置页弹窗背后的灰色遮罩没有盖满整个窗口、上方露出一块的问题。
- 修复历史记录中多条录音可同时播放、声音重叠的问题：现在同一时间只播放一条，点播新录音会自动暂停上一条并保留其进度，可从原位置继续播放。
- 修复进入设置、热词、外观等页面时，开关与选项（如悬浮窗长度、保留时长）会先显示默认值、再自己跳或滑到已保存状态的问题：现在这些控件读到保存值后才显示，出现即为最终状态，不再播放一次「像是被改过」的动画。
- 修复接近单次录音上限时看不到时间的问题：此前录到 4 分钟会弹出「单次记录最长300s」并把当前秒数顶掉，之后一整分钟既不知道已录多久、也不知道还剩多久。现在提示一闪即走，最后一分钟计时区改为显示剩余时间（最后 10 秒变暖橙并轻微脉动），并在设置页事先注明上限。
- 修复润色模式的快捷键在未保存时就已生效的问题：此前在新建界面设置快捷键会立即写入，取消新建后会残留一条指向不存在模式的快捷键，还可能把其它模式原有的同一组合键顶掉。现在快捷键随「保存」一起生效，取消则不留痕迹。
- 修复应用 Prompt 规则可能认错应用的问题：此前窗口标题里只要出现某个应用的名字就会命中它的规则，例如在 Outlook 里写一封标题含「Teams」的邮件，会被按即时聊天风格整理。现在改为按应用进程识别，标题只在进程无法区分时（如网页版应用）才作为依据。

### 变更
- 移除上一代 ONNX 本地识别引擎（sherpa-onnx）。本地模式需重新下载一次新格式模型；旧模型会自动清理、腾回磁盘空间。

---

## [0.1.3] - 2026-07-29

### 新增
- 本地识别模型支持自定义存储位置：模型文件较大，若系统盘空间紧张，可在「本地模式」设置里把模型改存到其它磁盘；更换目录后，已下载的模型会自动迁移到新位置。
- 历史记录支持手工修改：可直接编辑整理后的文本，改完点击其它地方即自动保存（无需保存按钮，按 Esc 取消）；编辑过的记录会用小图标标记，便于日后辨认。
- 新增「历史记录」开关（设置 → 通用，默认开启）：与他人共用电脑、不希望留下记录时可关闭；关闭后不再保存新的转写文本与录音（转写结果仍会照常插入到目标程序，不影响正常使用）。已有记录不会被删除，可在历史页手动清除。
- 意见反馈可查看处理进度：在应用内提交反馈后会给出反馈页链接；待作者审核公开后，可在该页面查看反馈内容、版本、时间以及作者的文字/截图回复与处理状态（如「计划中」「已完成」）。该页面仅供查看。

### 修复
- 修复用「右 Alt」等 Alt 键作为「按住说话」快捷键时，松开按键会呼出目标程序（如记事本）的菜单栏、使光标离开输入框，导致识别文本插不进去、日志却显示插入成功的问题。现已在系统按键层面正确拦截 Alt 键抬起，插入恢复正常。
- 修复数字规范化把口语里的「一点」（表示"稍微、一点点"）误当成时间「1 点」的问题：如「调宽一点」不再被改成「调宽 1 点」。整点时间仅在有明确时间信号时才转换（如「下午一点」「一点钟」「十二点」）。
- 修复关闭「AI 整理」后，默认的服务器模式下诊断页仍把「AI 校对」显示为正常的问题：现在无论哪种模式，关闭后都会明确显示为「未开启（极速模式）」。
- 修复设置里「麦克风」下拉只显示「系统默认」和一个通用项、看不到电脑上各麦克风名称的问题（新版系统 WebView 运行时收紧了设备枚举、未授予持久麦克风权限时会隐藏设备名与选择）。现已在应用层申请持久麦克风权限，恢复完整的麦克风列表与选择。
- 修复录音时「请靠近麦克风」提示在正常说话时也迟迟不消失的问题：此前需要连续约 0.5 秒不间断的正常音量才会清除，而说话时字与字之间的自然停顿会反复把进度重置，导致明明在说话也清不掉；现已容忍说话中的短暂停顿，正常说上一两句即可消除。

### 优化
- 优化录音时的麦克风提醒，区分「静音」与「声音小」：使用系统默认麦克风且它被系统静音时，录音悬浮窗会即时显示醒目的红色高警「麦克风已被静音」；完全采集不到声音（如选错设备、未授权）时显示「未检测到声音」；有声音但偏小时显示「请靠近麦克风」。此前只有单一的「请靠近麦克风」提示，麦克风被静音时也只提示声音小、不够直观。

---

## [0.1.2] - 2026-07-24

### 新增
- 新增「备份与迁移」，支持导出与导入两种备份：「配置」（仅设置、密钥、热词、提示词，文件小、便于日常备份）和「全部数据」（在配置基础上额外包含历史记录与录音，适合换机完整迁移）。导出直接保存到默认下载目录并就地显示文件路径与「打开所在文件夹」；导入采用应用内确认弹窗（会覆盖当前数据），完成后自动重启使更改生效。

### 优化
- 优化诊断页面「AI 校对」的状态显示：未开启时不再显示为绿色对勾（易被误解为已启用），改为中性的「未开启」状态，并温和提示依赖 AI 的功能当前不会生效、以及在哪里开启。

### 修复
- 修复长时间后台运行后偶发「一直显示处理中、最后超时且无结果」的问题：SayIt 主窗口常驻后台隐藏时，系统会节流其心跳定时器，导致与服务器的连接被判为空闲而每隔约 5 分钟被回收；若录音恰好在连接被回收的瞬间开始，本次识别就会丢失并超时。现已让后台定时器不再被节流，保持心跳正常、连接稳定。
- 识别超时时不再丢失录音：即使这次识别超时没有结果，录音也会保存到历史记录中，可随时点「重新识别」重试。
- 修复保存润色模式（以及切换麦克风、静音、剪贴板保护等设置）后软件卡顿 2-4 秒的问题：此前每次保存设置都会重新采集一次机器信息（系统区域、物理内存等），采集时会临时启动 PowerShell，其冷启动耗时叠加导致卡顿。现改为启动时采集一次并复用，且换用系统原生接口获取，保存设置不再卡顿。

---

## [0.1.1] - 2026-07-22

### 新增
- 「按住说话」与「免提模式」快捷键支持绑定鼠标中键（原有的鼠标侧键继续支持）。注意：绑定后鼠标中键的「打开新标签页 / 自动滚动」等原功能在 SayIt 运行时会被占用。

### 变更
- 数字规范化默认开启（把中文数字改写成阿拉伯数字，如 三点一四 → 3.14、百分之十五 → 15%）。已保存过该设置的用户不受影响。

### 修复
- 修复悬浮窗可能被其他置顶程序遮挡的问题：此前悬浮窗只在弹出瞬间置顶一次，当有其他「总在最前」的窗口（如某些播放器、会议共享工具条、置顶小工具）存在或后出现时，会盖住悬浮窗。现已在显示、前台窗口切换、以及显示期间周期性地主动重申置顶（不抢占焦点），确保悬浮窗稳定显示在最上层。（独占全屏程序仍会覆盖，属系统限制）
- 修复数字规范化把时间误当小数的问题：如「上午九点三十二分」曾被错转成「9.312」，现在正确处理为「9点32分」（「九点二十分」→「9点20分」）；同时修复句末小数（如 5.4、0.1.0）偶发不生效的问题。

### 优化
- 历史记录搜索命中的关键词现在会高亮显示（正文与 ASR 原文），更容易定位。
- 优化热词管理：分类内热词过多时自动折叠（默认显示前 30 个，可一键展开全部）；热词总数过多时给出可关闭的温和提示（热词并非越多越好）；热词说明的 ⓘ 改为表格，清晰列出各模型的热词支持情况与上限。
- 键盘快捷键设置新增信息说明（标题旁的 ⓘ）：集中说明可绑定的按键类型、选键建议，以及绑定鼠标侧键 / 中键会占用其原功能等注意事项。
- 优化低音量提醒（悬浮窗「请靠近麦克风」）：文案缩短并保证单行不换行；不再被瞬间的小声音清掉而闪烁（需持续说话约 0.5 秒才消失）；提醒不再过度频繁——还没听到过说话时静音约 2 秒提醒、已经正常说过话后停顿约 5 秒才提醒，避免正常思考停顿被反复催促。
- 识别结果为空时给出明确提示：以往悬浮窗会一闪而逝让人摸不着头脑，现在会用琥珀色短暂显示「未检测到有效声音」，让你明确知道这次没有识别到声音。

---

## [0.1.0] - 2026-07-17

### 新增
- 新增「流式实时字幕」（外观设置里开启）：说话时在悬浮窗上像输入法一样实时显示识别文字，边说边出字；说完的整段文本仍会照常交给 AI 整理。支持豆包（Doubao-Seed-ASR-2.0，需在火山控制台开通「流式语音识别 2.0」）与千问（qwen3-asr-flash-realtime，需在语音识别设置里填写百炼业务空间 ID）。
- 快捷键支持鼠标侧键：「按住说话」与「免提模式」均可绑定鼠标侧键（后退键 / 前进键）。在设置里点录制、按一下侧键即可绑定。
- 新增「口语化整理」AI 预设：把语音整理成微信、Teams 那样自然、不端着的文字，适合日常沟通和指挥 AI 编程助手，并自动规范常见中英文技术词的大小写。
- 应用 Prompt 规则新增内置应用：Codex、微信、QQ（默认关闭，可在「应用 Prompt 规则」里按需开启）。
- 诊断页新增「常见问题」自助排查，收录「识别文字没有插入到输入框」的检查步骤（光标位置、快捷键焦点、安全软件信任）。

### 修复
- 修复 AI 整理偶发"答非所问"：当语音内容本身是一句短指令（如"请执行"）时，个别情况下会被模型误当成对它的提问而反问用户。现已让服务器与本地都改用中性方式包裹待处理文本，不再夹带指令语气。
- 修复千问 ASR 短音频偶发把设置的热词原样吐出：极短/静音音频下模型可能回显热词上下文，现已识别并过滤这类结果。
- 修复诊断报告「覆盖记录终点」时间偶发带多余「[」符号的问题。

### 优化
- 优化插入文本时的剪贴板行为：插入的内容不再进入系统剪贴板历史（Win+V）与云剪贴板。此前「插入文本后保护剪贴板」在还原时反而会往剪贴板历史里留下记录，现已借助 Windows 的剪贴板排除机制彻底避免，历史更干净，你原来复制的内容也不受影响。

---

## [0.0.9] - 2026-07-16

### 新增
- 新增「文本处理」设置（热词页）：一组不依赖 AI 的文本格式开关，即使关闭 AI 整理也能规范识别结果。
- 数字规范化：把识别结果里的中文数字改写成阿拉伯数字，如 三点一四 → 3.14、百分之十五 → 15%、GPT五点四 → GPT 5.4（尽力而为，复杂场景仍建议开 AI 整理）。
- 去除句末标点：删掉每段结尾的句号、问号等收尾标点，句子中间的标点保留。
- 标点替换为空格：把文本里的所有标点统一替换为空格，适合当作关键词或粘贴到不希望带标点的地方。
- 智能分段做成可关闭的开关（默认开启）：极速模式（关闭 AI 整理）下把一大段语音按语义自动分成多个自然段。
- 一键检查更新：在「关于」页点击「检查更新」后直接下载并安装最新版本，无需再手动分步下载、安装。
- 首页公告栏：支持从服务器推送应用内通知（维护提醒、重要提示、手动更新引导等），无需更新客户端即可展示。

### 修复
- 修复自动更新的严重问题：安装新版本后不会自动重新打开，且重新打开又会再次触发更新、陷入反复报错、无法打开的死循环。本版本重写了更新的安装与重启流程，彻底修复该问题。**注意：该缺陷位于 0.0.8 版本自身的更新器，0.0.8 用户无法通过软件内自动更新升级到本版本，请前往官网 https://sayitapp.site 或 GitHub 手动下载安装 0.0.9；升级到 0.0.9 之后即可恢复正常的自动更新。**

### 优化
- 优化云 API 语音识别配置：App ID 仅在豆包 ASR 下显示，千问 ASR（含流式模式）只需填写百炼 API Key。
- 提升千问 ASR 流式连接与结果聚合的稳定性，减少异常回退到录完再识别的情况。
- 优化多显示器支持：在哪个屏幕的应用中开始录音，悬浮窗就会显示在对应屏幕底部。
- 优化写入失败时的识别结果悬浮窗：自动关闭时间缩短至 10 秒，点击复制后约 0.5 秒关闭。
- 增强悬浮窗与热键相关的诊断日志，便于定位偶发的「悬浮窗不出现」「热键无响应」等问题（正常运行时不产生多余日志）。

---

## [0.0.8] - 2026-07-09

### 优化
- 切换到云 API / 服务器模式后自动释放本地识别模型占用的内存，不再需要退出应用才能释放。
- 加快多处设置按钮的响应速度：语音识别与 AI 供应商的保存/测试连接、本地模型切换等操作，减少不必要的等待与重复请求。
- 大幅提升标题栏「AI 整理」开关与润色模式切换的响应速度，连续快速切换不再卡顿。
- 下载更新弹窗改为展示真实下载进度（按已下载字节数计算百分比），不再是固定动画。

### 新增
- 新增「插入文本后保护剪贴板」开关（默认开启）：插入完成后自动还原为插入前的剪贴板内容，避免占用剪贴板。

### 修复
- 提升服务器模式连接稳定性：修复长时间挂后台或空闲时，连接偶发反复中断、需要频繁自动重连的问题。
- 修复更新安装完成后不会自动重新打开应用的问题：现在会在安装完成后自动拉起新版本并跳转到「关于」页确认更新已生效。
- 修复应用启动自动检查更新与手动打开「关于」页检查更新可能同时触发、导致重复下载/安装的问题。
- 修复开启「录音时静音系统声音」后，就绪提示音偶发无声（首次或长时间未使用后）的问题。
- 修复远程桌面场景下误触发单键免提（Alt/Shift）的问题。

---

## [0.0.8] - 2026-07-08

### 新增
- 新增「录音时静音系统声音」开关（默认关闭）：按住说话时临时静音系统输出，避免外放声音被麦克风录入，松开即恢复。
- 润色模式支持自定义快捷键切换：为任意润色模式设置组合键（如 Alt+1），全局按下即可切换，切换时悬浮窗即时提示。
- 文本替换支持批量添加：粘贴多行内容（支持逗号、制表符、`=>` 分隔）一次性导入多条规则，已存在的原文会更新。
- 热键支持更多按键：菜单键、Pause、ScrollLock、Insert，便于避开易冲突的按键。
- 免提组合键（如 Alt+字母）设置时增加冲突提示，若已被其他程序占用会提醒更换。
- 关于页面新增「本次更新」板块，展示当前版本的主要更新内容。
- 服务器模式的服务地址增加说明提示（悬停信息图标查看），介绍默认体验服务器的用途与替代方案。
- 本地识别新增 Qwen3-ASR 0.6B 模型：支持多语种、准确度更高，适合对识别质量要求高的场景。
- 本地模型列表新增速度 / 准确度评分与「推荐」标识，方便按需选择合适的模型。
- 本地识别支持热词：使用 Qwen3-ASR 时，热词可提升专有名词与行业术语的识别准确率。
- 云 API 语音识别新增「小米 MiMo」供应商（mimo-v2.5-asr）：中英文与多种方言，自动标点，仅需填入 API Key。
- AI 润色新增「小米 MiMo」供应商（mimo-v2.5 / mimo-v2.5-pro），默认使用 mimo-v2.5。

### 优化
- 去除悬浮窗胶囊与卡片的投影，消除窗口边缘出现的灰色残影。
- 标题栏「AI 整理」按钮 hover 时显示当前润色模式；快捷键切换后 AI 整理页高亮实时更新。
- 意见反馈附带更多排错信息（当前应用/窗口/进程、识别参数、客户端环境），随「最后的转录」开关控制是否附带；不含音频与 IP。
- 首次安装直接进入设置向导，不再短暂闪现主页。
- 服务器模式下未连接后端时，按免提键改为显示简短告警，不再弹出无法关闭的悬浮窗。
- 托盘图标提示语更新为「随口说，出色写」。
- 长语音（超过约 30 秒）本地识别自动在停顿处分段处理，避免超长音频卡死或识别异常。
- 本地模型下载更稳更快：支持多下载源自动切换、断点续传，国内网络自动走加速通道。
- 小米 MiMo AI 润色默认关闭深度思考，回复更快。
- AI 供应商点击模型标签即刻生效并保存，无需再点保存按钮。

### 修复
- 修复在 CMD 中运行 Claude Code 等程序时无法插入文本的问题。
- 修复开启「录音时静音系统声音」后，按下热键没有就绪提示音的问题。
- 修复设置向导中更换热键后，测试仍需按默认键才生效的问题。
- 修复设置向导中更换免提热键后，首页提示仍显示旧按键、需切换页面才刷新的问题。
- 修复云 API 语音识别切换供应商时误带入其它供应商 API Key 的问题，各供应商密钥现独立保存。
- 修复 AI 润色实际运行、但未改动文本时，历史记录不显示所用 AI 供应商与模型的问题。
- 修复修改服务地址（含填错地址）后，左下角连接状态未刷新、仍显示已连接的问题。
- 修复同时打开两个客户端导致插入两份重复文本的问题，现改为单实例，二次启动会唤起已有窗口。
- 修复远程桌面等场景下，历史记录音频播放进度条卡住不动的问题。
- 修复切换 AI 供应商后可能仍沿用上一个供应商地址与模型的问题（如选了豆包却调用了其他模型）。
- 修复在 TRAE、Cursor、Windsurf 等编辑器中无法插入文本、只弹出兜底卡片的问题。
- 默认开启麦克风降噪，消除部分机器录音中的低频电流声/底噪。

---

## [0.0.7]

- 已发布基线版本。
