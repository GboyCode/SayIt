// 本次版本更新亮点（关于页面展示）。
// 每次发版时更新 version 与 items，保持与 CHANGELOG 同步。
// version 需与打包版本一致，关于页面仅在与当前版本匹配时展示，避免串版。
//
// 每条写成一句话：前半句说明功能，后半句补充解释，读起来更顺。

export interface ReleaseHighlights {
  version: string
  items: string[]
}

export const RELEASE_HIGHLIGHTS: ReleaseHighlights = {
  version: '0.1.3',
  items: [
    '本地识别模型支持自定义存储位置：系统盘空间紧张时可改存到其它磁盘，已下载的模型会自动迁移',
    '历史记录支持手工修改：可直接编辑整理后的文本，改完点其它地方即自动保存',
    '修复用「右 Alt」等 Alt 键做「按住说话」时会呼出记事本等程序菜单、导致文本插不进去的问题',
    '修复数字规范化把口语「一点」误当成时间「1 点」的问题，如「调宽一点」不再变成「调宽 1 点」',
    '录音时麦克风提醒更清楚：默认麦克风被静音会即时红色高警「麦克风已被静音」，并区分「未检测到声音」与「请靠近麦克风」',
  ],
}
