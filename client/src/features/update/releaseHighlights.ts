// 本次版本更新亮点（关于页面展示）。
// 每次发版时更新 version 与 items，保持与 CHANGELOG 同步。
// version 需与打包版本一致，关于页面仅在与当前版本匹配时展示，避免串版。
//
// 写法约定：
// · 按**重要性**排序：先「能力变强」，再「不再被坑」，最后「更顺手」；
// · 一条一句话。语气平实、偏书面，但不生硬：
//     - 别用宣传腔（「任你挑」「一头雾水」这类）；
//     - 也别太口语（「快了一大截」「自己跳一下」「测通不通、快不快」这类）；
//     - 校准点：像产品发布说明，不像聊天记录，也不像广告文案。
// · 说用户看得见的变化，不写模块名、字段名、「重构」这类内部词；
// · 短而不省：一眼能读完，但要让人知道"这对我意味着什么"；
// · 条数压在 10 条以内 —— 列太长等于没重点。

export interface ReleaseHighlights {
  version: string
  items: string[]
}

export const RELEASE_HIGHLIGHTS: ReleaseHighlights = {
  version: '0.1.4',
  items: [
    '本地识别更换新引擎，速度大幅提升，可选模型从 2 个增加到 5 个',
    '本地识别支持显卡加速，检测到显卡时自动启用，也可手动切回 CPU',
    'AI 服务改为卡片式管理：每张卡一套配置，点击即可切换，并可测试连通性与响应速度',
    '可为单个程序指定 AI 整理风格，用「检测当前应用」自动填写，无需手动输入进程名',
    '修复应用规则误匹配，例如在 Outlook 中撰写标题含「Teams」的邮件时不再套用错误规则',
    '接近单次录音 5 分钟上限时会显示剩余时间，设置页中也标明了该上限',
    '历史记录同一时间只播放一条录音，播放新录音会自动暂停上一条',
    '进入设置页时，开关不再出现一次自动跳变',
    '热词组、文本替换、润色模式支持拖拽排序，常用项可以排在前面',
    '开启 AI 整理后「格式规范」不再生效，排版交由 AI 处理',
  ],
}
