// 本次版本更新亮点（关于页面展示）。
// 每次发版时更新 version 与 items，保持与 CHANGELOG 同步。
// version 需与打包版本一致，关于页面仅在与当前版本匹配时展示，避免串版。
//
// 写法约定：
// · 按**重要性**排序：先「能力变强」，再「不再被坑」，最后「更顺手」；
// · 一条一句话。语气平实、偏书面，但不生硬：
//     - 别用宣传腔（「任你挑」「一头雾水」这类）；
//     - 也别太口语（「快了一大截」「自己跳一下」「测通不通、快不快」这类）；
//     - 校准点：像产品发布说明，不像聊天记录，也不像广告文案。
// · 说用户看得见的变化，不写模块名、字段名、「重构」这类内部词；
// · 短而不省：一眼能读完，但要让人知道"这对我意味着什么"；
// · 条数压在 10 条以内 —— 列太长等于没重点。

export interface ReleaseHighlights {
  version: string
  items: string[]
}

export const RELEASE_HIGHLIGHTS: ReleaseHighlights = {
  version: '0.1.5',
  items: [
    '语音识别服务改为卡片式管理：一张卡一套配置，同一家可保存多份，点击即可切换，并能测试可用状态与实测耗时',
    '豆包语音识别支持新版控制台的接入方式，只需填写一个 API Key',
    '修复选择本地识别模型后界面无响应的问题，加载期间会显示明确的加载提示',
    '修复豆包仅填写一个密钥时的鉴权错误',
    '修复仅开通「并发版」资源的豆包账号无法使用普通识别的问题',
    '千问实时识别缺少业务空间 ID 时会提前提示，不再等到录音结束才失败',
    '识别服务的检测结论超过 24 小时会提示重新测试，避免把过期结果当作可用',
    '「检测当前应用」的等待时间从 3 秒延长到 5 秒',
    '设置页中说明了录音或识别处理期间可按 Esc 取消本次识别',
    '悬浮窗长度的选项改按「长 / 中 / 短」排列',
  ],
}
